Constants
---------
- Demonstrates the use of a constant on the starter file "app.js"
    - The browser reads the content of a HTML document
        - From top to bottom
        - Executes (renders) each line that is read

    - JavaScript code's default behavior
        - Code is parsed (read) from top to bottom
        - Each line read is compiled and executed "on-the-fly"
