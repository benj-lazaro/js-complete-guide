// document and window Demo

// Display the content of the "document" object
console.log(document);

// Access the "document" object's READ-ONLY properties
console.dir(document);

// Display the content (properties & method) of the "window" object
console.log(window);
