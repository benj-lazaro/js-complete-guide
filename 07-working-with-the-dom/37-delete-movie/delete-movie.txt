Deleting Movie Elements
-----------------------
- Implement logic that removes a movie entry from the list
    - By clicking on the movie entry itself


- In the practice project (assets/scripts/app.js)
    - Add an Event listener w/in the Arrow function "renderNewMovieElement()"
        - Attach the method ".addEventListener()" to the object "newMovieElement"
            - 1st parameter = "click"

            - 2nd parameter = "deleteMovieHandler.bind()"
                - 1st parameter = "null"
                - 2nd parameter = "id"

        - Refactor the parameter list to include "id"
   
    - Refactor the Arrow function "renderNewMovieElement()"
        - Add a parameter variable "id" & position it as the 1st

    - Switch to the Arrow function "addMovieHandler()"
        - On the local constant "newMovie"
            - Add a new property named "id"
            - Assign a value generated by "Math.randon().toString()"

        - On the call to "renderNewMovieElement()"
            - Pass "newMovie.id" as argument value

    - Define an Arrow function named "deleteMovieHandler"
        - Located outside the Arrow function "renderNewMovieElement()"
            - Declare the parameter variable "movieId"

        - Declare a local variable named "movieIndex"
            - Assign an initial value of 0

        - Use a "for..of" loop to search for the matching index value w/ the value of "movieId"
            - Check if the value of "movie.id" is equal to the value of the parameter "movieId"
                - If equal, break the loop
                - Otherwise, increment the value of "movieIndex" by 1

        - Use the method ".splice()" on the array "movies"
            - 1st parameter = "movieIndex"
            - 2nd parameter = 1

        - Select the HTML element <ul> w/ an attribute "id" of "movie-list"
            - Store the returned Element node on a local constant named "listRoot"

            - Use the property ".children" on the Element node "listRoot"
                - Pass "movieIndex" as index selector
                    - e.g. "listRoot.children[movideIndex]"

                - Chain the method ".remove()"
                    - e.g. "listRoot.children[movideIndex].remove()"
            
            - NOTE: Alternatively, use the method ".removeChild()"
                - Pass "listRoot.children[movieIndex]" as argument value
                    - e.g. "listRoot.removeChild(listRoot.children[movieIndex])"
                
    - Save the changes

    - Load / reload the browser
        - Add a new movie item

        - Delete a movie item by clicking on it


    

