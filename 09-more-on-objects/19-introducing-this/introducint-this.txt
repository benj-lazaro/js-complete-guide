Introducting "this"
-------------------
- The keyword "this" refers to
    - The piece of code responsible for calling a method

    - Really easy way to determine who "this" is referring to
        - It is the thing (object) in front of the method being called 
        
        - NOTE: See practice project's line 47 & 95


- On the practice project (assets/scripts/objects.js)
    - In the creation of the object "movie" using Object literal notation
        - Define a new method named "getFormattedTitle"
            - Assign an anonymous (NOT an Arrow) function as value

        - The anonymous function
            - Returns the value of the "this" object's property "info.title"
            - Chains the string method ".toUpperCase()" to format in uppercase

            - NOTE: The keyword "this" represents the object "movie"

    - Switch to the helper function stored in the global constant "renderMovies"
        - Comment the line that extracts the property "title" from
            - The nested reference value assigned in the property "info"

        - On the following line of code
            - Change the assigned expression to the local variable "text" to:
    
                let text = movie.getFormattedTitle() + " - ";        

    - Save the changes

    - Load / reload the browser
        - Test the implemented changes