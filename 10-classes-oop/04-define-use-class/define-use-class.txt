Defining & Using A First Class
------------------------------
- A Class allows to easily build an object
    - It serves as a theoretical "blueprint" of an object
        - Defines an object's properties & methods

    - Easily creates multiple, similar "looking" objects
        - Contains different data (values)
        - Retains identical structures


- An object created from a Class is referred as an "instance" of that Class
    - It serves as an alternative (NOT REPLACEMENT) for Object literal notation


- To create a Class
    - Use the keyword "class" followed by a name
        - The 1st letter of EVERY WORD MUST be in UPPERCASE
        - Whitespace characters in-between words is NOT allowed
    
    - Followed by a pair of "{ }"

    - Inside the pair of "{ }"
        - Define the Class field name (i.e. property)
            - Followed by a "=" operatpr
            - Then a default value
            - Terminate each field name w/ a ";"

        - NOTE: NOT assigning a default value to a Class field
            - JavaScript implicitly assigns an "undefined" value

    Syntax:
    Syntax:
    class <class_name> {
        <field_name> = <default_value>;
        ...
        <field_name>;
    }


- To create an instance (instantiate) of a Class
    - Use the keyword "new" followed by the Class name w/ a pair of "( )"
        - Terminated w/ a ";"
        - JavaScript implicitly returns a Class-based object 

        - NOTE: Often the returned object is assigned to a constant 

    Syntax:
    new <class_name>();
    const <constant_name> = new <class_name>();


- On the practice project (assets/scripts/app.js)
    - Create a Class named "Product" w/ the
        - Following Class fields
            - "title"       = default string value "DEFAULT"
            - "imageUrl"    = NO assigned default value (i.e. undefined)
            - "price"       = NO assigned default value (i.e. undefined)
            - "description" = NO assigned default value (i.e. undefined)

    - Instantiate the Class "Product"
        - Pass the returned object as argument to "console.log()"

    - Save the changes
    
    - Load / reload the browser
        - Launch the browser's JavaScript console

        - The object "Product" is displayed 
            - Including its properties & default values
