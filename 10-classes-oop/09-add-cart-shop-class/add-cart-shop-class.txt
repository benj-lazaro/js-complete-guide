Adding A Cart & Shop Class
--------------------------
- Next is to create new Classes in the web app
    - Class "ShoppingCart"
        - Contains "ProductItem" object(s) to be checked out
        - Calculate the total amount
        - Render an HTML element <button> w/ the content "Order Now!"

    - Class "Shop"
        - Renders the objects "ProductList" & "ShoppingCart" in the DOM


- On the practice project (assets/scripts/app.js)
    - Create a new Class named "ShoppingCart"
        - Define a Class field named "items"
            - Assign w/ an empty array

        - Define a method named "render"
            - Create an HTML element <section>
                - Using the method "document.createElement()"
                - Pass "section" as argument
                - Store the returned Element node in the local constant "cartEl"

            - Chain the property ".className"
                - Assign the value "cart"

            - Chain the property ".innerHTML"
                - Assign the following HTML mark-up enclosed w/ a pair of ` `
                    - An HTML element <h2> w/ the content of "Total: \$${0}"
                    - An HTML element <button> w/ the content of "Order Now!"

                - NOTE: The "${0}" is tentaive placeholder for the total amount of items
                    - The logic to make this value dynamic will be later implemented

        - Return the Element node "section" stored in 
            - The local constant "cartEl" using a "return" statement

        
    - Create another Class named "Shop"
        - Define a method named "render"

        - Instantiate the Class "ShoppingCart"
            - Store the returned object in the local constant "cart"

        - Call the method "render()" stored in the local constant "cart"
            - Store the returned Element node "section" in the local constant "cartEl"


    - On the last line of code; outside the Class "Shop" 
        - Select & cut the lines of code that
            - Instantiates the Class "ProductList"
                - Stores the created object in the constant "productList"

            - Calls the method "render()" stored in the constant "productList"


    - Switch back to the method "render()" of the Class "Shop"
        - Paste the lines of code

        - Save the returned Element node "ul" from the method "render()" in "productList"
            - To the local constant "prodListEl"


    - Switch to method "render()" of the Class "ProductList"
        - Select & cut the declaration of the local constant "renderHook"


    - Switch back to the method "render()" of the Class "Shop"
        - Paste the line of code above the declaration of the local constant "cart"


    - Switch to method "render()" of the Class "ProductList"
        - Refactor the line of code where the 
            - (removed) local constant "renderHook" chains the method ".append()"

        - To return the Element node "ul" stored in the local constant "prodList"
            - Using a "return" statement


    - Switch back to the method "render()" of the Class "Shop"
        - Chain the method ".append()" to the local constant "renderHook"
            - Pass "cartEl" as argument
            - Renders the shopping cart in the DOM

        - Chain the method ".append()" to the local constant "renderHook"
            - Pass "prodListEl" as argument
            - Renders the product list in the DOM


    - Move outside the Class "Shop" & at the last line of code
        - Instantiate the Class "Shop"
            - Store the created object in the global constant "shop"

        - Call the method "render()" stored in the global constant "shop"


    - Save the changes

    - Load / reload the browser
        - Launch the browser's JavaScript console

    - NOTE: Application behavior
        - Clicking the button "Order Now!" in the rendered shopping cart 
            - Does NOTHING

        - Clicking the button "Add to Cart" in the rendered product list 
            - Displays "Adding to cart" on the browser's JavaScript console
            - Displays the object of the selected "Product" object
            
            - The value of "Total" in the rendered shopping cart does NOT change