class Person {
  name = "Max";
}

// Instantiate Class Person into an object
const person = new Person();
console.log(person);

// Check if object "person" is an instance of the Class "Person"
console.log(person instanceof Person);
