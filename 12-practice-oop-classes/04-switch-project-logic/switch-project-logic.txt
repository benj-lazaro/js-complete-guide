Starting With The "Switch Project" Logic
----------------------------------------
- On the Class "ProjectList"
    - Iterates through the selected Element node(s) "li"
        - From the local constant "projectItems"

    - For each Element node "li" read
        - Instantiate the Class "ProjectItem"
        - Access the Element node "li" property "id" & pass it as an argument value 

    
- On the Class "ProjectItem"
    - Create a Constructor method that accepts two (2) inputs
        - The HTML attribute "id" of the Element node "li" read
        - The name of the callback function

    - Create two (2) Class methods
        - One that triggers the switch of a "ProjectItem" object
            - To move from one instance of the Class "ProjectList" to another

        - One that handles the button "More Info" of a "ProjectItem" object


- On the Class "ProjectItem" Constructor method
    - Create a Class property "id"
        - Assign the parameter variable "id" as value

    - Create a Class methods based on the previous defined Class methods
    
    - NOTE: Both Class property & methods prefixed w/ the keyword "this"


- On the practice project (assets/scripts/app.js)
    - On the Class "ProjectList"
        - Define a Class field named "projects"
            - Initialized w/ an empty array

            - NOTE: Stores an array of "ProjectItem" objects
        

    - On the Class "ProjectList" Constructor method
        - Implement a "for..of" loop to iterate through 
            - Each Nodelist stored on the local constant "projectItems"

            - Each Element node "li" read
                - Store it on the local constant "projectItem" (singular)

        - Inside the "for..of" loop
            - For each Element node "li" read
                - Instantiate a "ProjectItem" object 

            - Access the Element node "li" property "id"
                - Pass it as argument value (i.e. "projectItem.id")

                - NOTE: Element node's attribute "id" === property "id"

            - Push the created "ProjectItem" object to 
                - The Class field "projects" using the Array method ".push()"

        - Pass the Class field "projects" (as a Class property) 
            - To the "console.log()" as an argument value
            - Displays "ProjectItem" objects


    - On the Class "ProjectItem"
        - Create a Constructor method w/ 
            - A parameter variable named "id"

        - Create two (2) Class methods
            - "connectSwitchButton"
                - Handles the "Finish" or "Activate" button of a "ProjectItem" object
                    - Triggers the move of a "ProjectItem" object to a different
                    - Instance of the Class "ProjectList" 
                    - (i.e. "Active Projects", "Finished Projects")

            - "moreInfoButton"
                - Handles the button "More Info" of a "ProjectItem" object

    
    - On the Class "ProjectItem" Constructor method
        - Create a Class property "id"
            - Assign it w/ the parameter variable "id"
        
        - Create two (2) Class methods that is named after
            - The (previously) defined Class methods
                - "connectSwitchButton"
                - "moreInfoButton"

            - Each prefixed w/ the keyword "this"


    - On the Class "ProjectItem" method "connectSwitchButton()"
        - Select ALL HTML element <li> by their attribute "id"
            - Using the method "document.getElementById()"

            - Pass the Class property "this.id" as argument value

            - Store the returned Element node "li"
                - To the local constant "projectItemElement"

        - Select the last HTML element <button> from the selected Element node "li"
            - Using the method ".querySelector()"
                - Chained to the local constant "projectItemElement"

            - Pass the CSS selector "button:last-of-type" as argument value

            - Store the returned Element node "button"
                - To the local constant "switchBtn"

            - NOTE: This refers to the "ProjectItem" buttons "Finish" & "Activate"

        - Attach an Event listner to the local constant "switchBtn"
            - Using the method ".addEventListener()"

            - Pass the Event "click" as the 1st argument value

            - NOTE: The 2nd argument (callback function) to be implemented on the next lesson

            - This facilitates the "trigger" that switches a "ProjectItem" object
                - To move between two (2) instances of the Class "ProjectList"
                - (i.e. "Active Projects" & "Finished Projects")

            - A callback function (to be implemented) will be
                - Attached & managed by the Class "ProjectList"


    - On the Class "ProjectList"
        - Create two (2) Class methods
            - "addProject()"
                - Handles the adding of a "ProjectItem" object
                    - To a new instance of the Class "ProjectList"

            - "switchProject()"
                - Assign it w/ the parameter variable "projectId"
                
                - Handles the move of a "ProjectItem" object between
                    - Different instances of the Class "ProjectList"
                    - (i.e. "Active Projects" & "Finished Projects")

                - NOTE: This is the callback function triggered by 
                    - The Class "ProjectItem" method "connectSwitchButton()"

    
    - On the Class "ProjectList" method "switchProject()"
        - SOLUTION #1

        - Access the Class field "projects" (as a Class property)
            - Chain the method 



    - On the Class "ProjectList" method "switchProject()"
        - Define a parameter variable "projectId"

        - Solution #1: Longer implementation
            - Identify the index position of the "ProjectItem" object
                - Whose button "Finish" or "Activate" has been clicked
                - That is stored in the Class field "projects"

            - Access the Class field "projects" (as a Class property)

            - Chain the method ".findIndex()"
                - Pass an Arrow function as argument value

            - The Arrow function
                - Define w/ a parameter variable "p"
                    - Stores the Element node "li" read 

                - Compares the Element node property "id" w/ 
                    - The parameter varaible "projectId"

                - If STRICTLY EQUAL
                    - Assign the returned index value to the local constant "projectIndex"

            - Access the Class field "projects" (as a Class property)
                - Chain the method ".splice()"

                - Pass the argument values
                    - 1st = local constant "projectIndex"
                    - 2nd = 1

                - Removes the corresponding "ProjectItem" object
                    - From the Class field "projects"


        - Solution #2: Shorter implementation
            - Access the Class field "projects" (as a Class property)
                - Chain the method ".filter()"

                - Pass an Arrow function as argument value 

            - The Arrow function
                - Define w/ a parameter variable "p"
                    - Stores the Element node "li" read 

                - Compares the Element node property "id" w/ 
                    - The parameter varaible "projectId"

                - If NOT STRICTLY EQUAL
                    - Assign the returned index value to the local constant "projectIndex"
                   
                    - Keeps ALL Element node "li" read whose attribute "id" 
                        - Is NOT EQUAL to the parameter variable "projectid"

                - NOTE: This "removes" a "ProjectItem" object from the instance of
                    - The Class "ProjectList" it initially belongs to

            - Assign the returned array back to the Class field "projects"
                - Effectively overwriting the old Array of "ProjectItem" objects


    - Save the changes


    - NOTE: The Class "ProjectList" method "addProject()" 
        - Will be implemented on the next lesson
