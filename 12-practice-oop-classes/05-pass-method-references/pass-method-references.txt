Passing Method References Around
--------------------------------
- Implement the logic for the callback function of
    - The Class "ProjectItem" method "connectSwitchButton"


- On the Class "ProjectList" Constructor method
    - Create a new Class property named "type"
        - Assign the parameter variable "type" as value


- On the practice project (assets/scripts/app.js)
    - On the Class "ProjectList" Constructor method
        - Create a Class property named "type"
            - Assign the parameter variable "type" as value

        - NOTE: Identifies the Class "ProjectList" instance that
            - A "ProjectItem" object currently belongs to


    - On the Class "ProjectList"
        - Create a Class method named "setSwitchHandlerFunction" w/
            - A parameter variable named "switchHandlerFunction"


   - On the Class method "setSwitchHandlerFunction()"
        - Create a Class property named "switchHandler"
            - Assign the parameter variable "switchHandlerFunction" as value

        - NOTE: This method set the Class "ProjectList" instance & method that
            - A "ProjectItem" object will switch to when its corresponding
            - DOM button (i.e. "Finish" or "Activate") is clicked       
        

    - On the Class "ProjectList" method "switchProject()"
        - Access the Class property "switchHandler" defined w/in the
            - Class method "setSwitchHandlerFunction()" like a method
            - (i.e. this.switchHandler())

        - Access the Class field "projects" (like a Class property)
            - Pass it as argument to the Class property "this.switchHandler"
            - Chain the method ".find()"
            - Pass an Arrow function as argument value
            - (i.e. this.switchHandler(this.projects.find(...) => ...))

        - The Arrow function comes w/ a parameter variable "p" that
            - Holds the current "ProjectItem" object read from the Class field "projects"

            - Compare the property "id" of the "ProjectItem" object w/ 
                - The current value of the parameter variable "projectId"

            - IF BOTH are STRICTLY EQUAL, store the "ProjectItem" object as value
                - To the Class property "switchHandler"

        - NOTE: This method finds the matching "ProjectItem" object
            - Then removes it from the current "ProjectList" instance it belongs to


    - On the Class "App"
        - Chain the method ".setSwitchHandlerFunction()" to 
            - The local constant "activeProjectList"
            - NOTE: Holds an instance of the Class "ProjectList"
        
        - Pass the local constant "finishedProjectList" as argument value
            - Chain the method ".addProject"
            - Chain the method ".bind()"
            - Pass the same local constant "finishedProjectList as argument value

        - Repeat the same for the local constant "finishedProjectList" w/
            - The local constant "activeProjectList" as argument value
        
        - NOTE: This sets a switch handler for the instantiated Class "ProjectList"
            - It contains the method & other "ProjectList" instance that allows its
            - Own "ProjectItem" object(s) to switch over to when its corresponding
            - DOM button (i.e. "Finish" or "Activate") is clicked


    - On the Class "ProjectList" method "addProject()"
        - For the current implementation
            - Pass the keyword "this" as argument value to "console.log()"

        - NOTE: The keyword "this" holds the "ProjectList" instance that
            - A "ProjectItem" object will switch over to when its DOM button is clicked


    - On the Class "ProjectItem" Constructor method
        - Add a new parameter variable named "updateProjectListsFunction"

        - Create a new Class property "updateProjectListsHandler"
            - Assign the parameter variable "updateProjectListsFunction" as value

        - NOTE: The Class property "updateProjectListsHandler" points to
            - The Class "ProjectList" method "switchProject()"

            - This is evident on the refactored line of code on how the
            - Class "ProjectList" Constructor method instantiates a "ProjectItem" object
            - As seen on the following step


    - On the Class "ProjectList" Constructor method
        - Refactor the line of code that instantiates the Class "ProjectItem"

        - On the "for..of" loop
            - Pass the Class method "switchProject()" as 2nd argument value
                - When instantiating a Class "ProjectItem"
                    - NOTE: Omit the pair of "( )"

                - Chain the method ".bind()"
                    - Pass the keyword "this" as argument value

            - NOTE: The keyword "this" refers to the Class "ProjectList" instance & method 
                - That a "ProjectItem" object will switch to when its DOM button is clicked
                - (i.e. "Finish" or "Activate") 

                - Assigned as the 2nd argument value of the Class "ProjectItem"
                    - On the parameter variable "updateProjectListsFunction"

                - Stored on the Class "ProjectItem" property "updateProjectListsHandler"


    - On the Class "ProjectItem" method "connectSwitchButton()"
        - Pass the Class property "updateProjectListsHandler" as
            - The callback function to the Event listener attached
            - To the local constant "switchBtn"
            - DOM button (i.e. "Finish" or "Activate")


    - Save the changes
        - Load / reload the browser

        - Clicking the button "Finish" of a "ProjectItem" object
            - Under the group "Active Projects"

            - Returns a "ProjectList" object w/
                - The property "type" set to "finished"

        - Clicking the button "Activate" of a "ProjectItem" object
            - Under the group "Finished Projects"
            
            - Displays a "ProjectList" object w/ 
                - The property "type" set to "active"