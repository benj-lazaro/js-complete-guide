Passing Method References Around 
--------------------------------
- Implement the logic for the callback function of
    - The Class "ProjectItem" method "connectSwitchButton"

    - On the Class "ProjectList" Constructor method
        - Create a new parameter variable that accepts the switchHander 
            - Points to the callback function of a "ProjectItem" object that
            - Would allow it to switch to another instance of the Class "ProjectList"


- On the practice project (assets/scripts/app.js)
    - On the Class "ProjectList" Constructor method
        - Create a Class property "this.type"
            - Assign the parameter variable "type" as value

          
    - On the Class "ProjectList" method "switchProject()"
        - Call the Class property "this.switchHander" like a method

        - Pass the Class field "projects" as an argument value
            - Chain the method ".find()"
                - Pass an Arrow function as argument value

            - The Arrow function comes w/ a parameter variable "p"
                - Stores the current Element node "li" read

                - Compare the Element node "li" property "id" w/
                    - The parameter variable "projectId"

                - Check if BOTH are STRICTLY EQUAL then
                    - Assign the matching Element node "li" to
                    - The Class property "switchHandler"


    - On the Class "ProjectList"
        - Create a new Class method "setSwitchHandlerFunction" w/
            - The parameter variable "switchHandlerFunction"

        - Create a new Class property "this.switchHandler"
            - Assign the parameter variable "switchHandlerFunction" as value
            - NOTE: This serves as a pointer to the callback function of a "ProjectItem" object


    - On the Class "App"
        - Call the method ".setSwitchHandlerFunction()" on the local constant "activeProjectList"
            - Pass the Class "ProjectList" instance where the "ProjectItem" object will switch to
                - As argument value (i.e. finishedProjectList)

            - Chain the method ".addProject()"

            - Then the method ".bind()"
                - To bind the keyword "this" inside of the method ". addProject()"

            - Pass "finishedProjectList" as argument value


        - Call the method ".setSwitchHandlerFunction()" on the local constant "finishedProjectList"
            - Pass the Class "ProjectList" instance where the "ProjectItem" object will switch to
                - As argument value (i.e. activeProjectList)

            - Chain the method ".addProject()"

            - Then the method ".bind()"
                - To bind the keyword "this" inside of the method ". addProject()"

            - Pass "finishedProjectList" as argument value


    - On the Class "ProjectList" method "addProject()"
        - Pass the keyword "this" as argument value to "console.log()"


    - On the Class "ProjectItem" Constructor method
        - Create an additional parameter variable "updateProjectListsFunction"

        - Create a new Class property "updateProjectListsHandler"
            - Assign the parameter variable "updateProjectListsFunction" as value
            
            - NOTE: This points to corresponding callback function


    - On the Class "ProjectList" Constructor method
        - Refactor the instantiation of the Class "ProjectItem"

        - Pass the Class method "switchProject()" as a 2nd argument value
            - Chain the method ".bind()"
            - Pass the keyword "this" as argument value
            
            - NOTE: The keyword "this" refers to the corresponding Class "ProjectList"
                - NOT the "ProjectItem"


    - On the Class "ProjectItem" method "connectSwitchButton()"
        - Pass the Class property "this.updateProjectListsHandler" as the callback function
            - For the attached Event listener of the local constant "switchBtn"


    - Save the changes
        - Load / reload the browser

        - Clicking the button "Finish" of a "ProjectItem" object
            - Under the group "Active Projects"

            - Displays a "ProjectList" object w/ 
                - The value of the property "type" set to "finished"

        - Clicking the button "Activate" of a "ProjectItem" object
            - Under the group "Finished Projects"
            
            - Displays a "ProjectList" object w/ 
                - The value of the property "type" set to "active"